import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  box: {
    backgroundColor: '#F2F0F2',
    marginTop: 60,
    height: 320,
    marginHorizontal: 10,
    borderRadius: 10,
    elevation: 30,
    borderWidth: 0.5,
  },
});
