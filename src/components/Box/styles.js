import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  box: {
    backgroundColor: '#F2F0F2',
    marginTop: 60,
    minHeight: 320,
    marginHorizontal: 10,
    borderRadius: 10,
    elevation: 10,
    borderWidth: 0.5,
  },
});
