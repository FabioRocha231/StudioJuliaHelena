import { StyleSheet, Dimensions } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  imageContain: {},
  image: {
    position: 'absolute',
    left: 153,
    top: -40,
    borderRadius: 50,
    borderWidth: 3,
    borderColor: '#D64E85',
  },
});
