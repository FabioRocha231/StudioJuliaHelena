import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  imageWrapper: {
    width: '100%',
    height: 300,
    alignItems: 'center',
    backgroundColor: '#F2C8C4',
  },

  text: {
    marginTop: 10,
    backgroundColor: '#6DDDF2',
    color: '#ffffff',
    elevation: 5,
    width: 200,
    borderRadius: 20,
    fontSize: 18,
    textAlign: 'center',
  },
});

export { styles };
